"""Shared model primitives are imported from explicit submodules.

The package initializer intentionally does not register the retired workflow tables when an
adapter imports :mod:`app.models.enums`.
"""
